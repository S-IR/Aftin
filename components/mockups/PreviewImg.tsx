import React from "react";
import { mockupTemplate } from "../../constants/mockups/mockupTemplates";

interface props {
  placementData: mockupTemplate;
}

const PreviewImg = ({ placementData }: props) => {
  return <div></div>;
};

export default PreviewImg;
